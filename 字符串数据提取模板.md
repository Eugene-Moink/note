# 字符串数据提取模板笔记

## 适用场景

- **字符过滤与重组**：从给定的混合字符串中提取特定类型的字符（如数字、大写字母、小写字母等）
- **保持原序**：提取后的字符必须按照在原字符串中出现的先后顺序排列
- **简单的筛选累加**：对提取出的字符进行累加（如拼接成新串、求和等）

## 核心模板

```cpp
// 从字符串中提取数字字符，并按原顺序拼接
#include<bits/stdc++.h>
#define endl "\n"
using ll = long long;
using namespace std;

int main()
{
    string s;
    cin >> s;

    string ans = "";                     // 存放提取结果的字符串

    for (ll i = 0; i < s.size(); i++)   // 遍历原字符串
    {
        if (s[i] >= '0' && s[i] <= '9') // 判断当前字符是否为数字
        {
            ans += s[i];                // 符合条件的字符拼接到结果串尾部
        }
    }

    cout << ans << endl;
    return 0;
}
```

## 关键注意事项

### 1. 判断条件的正确写法

```cpp
// ❌ 错误写法 1：用错逻辑运算符
if (s[i] >= '0' || s[i] <= '9')  // 会导致所有字符都被提取，因为所有字母都 >= '0'

// ❌ 错误写法 2：比较对象用错
if (i <= '9')                    // 将循环变量 i 与字符 '9' 比较，没有任何意义

// ✅ 正确写法
if (s[i] >= '0' && s[i] <= '9')  // 同时满足 >= '0' 且 <= '9' 才是数字字符
```

### 2. 字符串拼接细节
- 直接使用 `ans += s[i];` 即可将字符追加到字符串末尾，无需转换类型。

### 3. 进阶写法：利用内置函数
如果使用了万能头文件 `<bits/stdc++.h>`，可以直接调用 `isdigit()` 简化判断条件：
```cpp
if (isdigit(s[i]))
{
    ans += s[i];
}
```
（`isalpha()`、`isupper()` 等函数用法类似，可快速判断字符类型）

## 快速判断方法

遇到字符串处理问题时问自己：

1. **是否只需要提取特定字符？** → 数字 / 大写 / 小写 / 自定义集合
2. **是否严格要求保持原始顺序？** → 答案必须是提取顺序的拼接
3. **提取后是否需要进一步处理？** → 仅拼接 / 求和 / 统计个数

**全部是 Yes** → 直接套用字符串遍历 + `if` 条件过滤模板！

## 典型变体

- **提取数字并求和**：`if (isdigit(s[i])) { sum += s[i] - '0'; }`
- **提取所有大写字母**：`if (s[i] >= 'A' && s[i] <= 'Z')`
- **提取所有小写字母**：`if (s[i] >= 'a' && s[i] <= 'z')`
- **提取特定字符**：`if (s[i] == '+' || s[i] == '-')` （提取符号）
- **过滤掉指定字符**：`if (s[i] != ' ')` （提取原串去掉空格后的所有内容）

记住：**遍历原串 + 条件筛选 + 拼接至答案串 = 字符串数据提取**