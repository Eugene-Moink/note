## 0-based 下标计算笔记

### 基本原则
- 所有下标从 **0** 开始
- 数组长度为 `n`，最后一个下标是 `n-1`
- 计算“元素个数”时，**包含两端则加 1**

---

### 常用公式表

| 目标 | 公式 | 记忆点 |
| :--- | :--- | :--- |
| 从开头到下标 `r`（包含 `r`）的元素个数 | `r + 1` | 因为下标从 0 开始，`0..r` 共 `r+1` 个 |
| 从下标 `l` 到末尾（包含 `l`）的元素个数 | `n - l` | 末尾是 `n-1`，个数为 `(n-1)-l+1 = n-l` |
| 从下标 `l` 到下标 `r`（包含两端）的元素个数 | `r - l + 1` | 通用区间长度公式 |
| 从开头到下标 `l` **之前**（不包含 `l`）的元素个数 | `l` | 即 `0..l-1`，共 `l` 个 |
| 从下标 `r` **之后**（不包含 `r`）到末尾的元素个数 | `n - 1 - r` | 即 `r+1..n-1`，共 `(n-1)-(r+1)+1 = n-1-r` |

---

### 删元素步数专用（常见题型）

| 操作 | 公式 | 说明 |
| :--- | :--- | :--- |
| 从左边删到下标 `l`（**包含** `l`） | `l + 1` | 删掉 `0..l` 全部 |
| 从右边删到下标 `r`（**包含** `r`） | `n - r` | 删掉 `r..n-1` 全部 |
| 左删到 `l` + 右删到 `r`（`l < r`） | `(l + 1) + (n - r)` | 两边同时删，无重叠 |

---

### 记忆口诀
> **“包含端点加一，不算端点就原样。”**

- 看到“包含” → 记得补 `+1`
- 看到“到末尾” → 用 `n - 下标` 自动包含
- 看到“之前”“之后” → 直接减，不加也不减

---

### 示例（`n=5, l=1, r=3`）

| 公式 | 结果 |
| :--- | :--- |
| `r + 1` | `3+1=4` |
| `n - l` | `5-1=4` |
| `(l+1)+(n-r)` | `2+2=4` |

验证：`0..3` 共 4 个；`1..4` 共 4 个；左删 `0..1` 共 2 个，右删 `3..4` 共 2 个，总 4 个。✅

---

只要用这套 0-based 公式，再也不需要纠结 1-based 的位置，也不会漏加或少加 1。