# 模板笔记：二维网格连通块问题（DFS / 淹没染色法）

## 1. 适用场景

- 题目给出一张 n × m 的网格地图（例如 100×100）。
- 地图上有两种状态（如水 `W` 和陆地 `.`，或 0 和 1）。
- 要求统计**连通的区块共有多少个**。
- 连通规则通常是 4 连通（上下左右）或 8 连通（包含对角线）。

## 2. 核心解题思路（3步走）

- **第一步：准备地图与方向导航**：用二维数组/字符串存图，用 `dx` 和 `dy` 方向数组代替繁琐的 `if` 判断。
- **第二步：DFS 搜索（染色/淹没法）**：一旦找到一个“目标块”（如 `W`），立刻调用 DFS，将当前块以及所有和它连通的相邻目标块**全部改成陆地（如 `.`）**。这个过程就像倒墨水，把一整块水域全部染色填平。
- **第三步：大循环普查**：在主循环里遍历整张地图。只要遇到一个未被染色的目标块，答案 `ans++`，并立刻调用 DFS 淹没它。最终答案就是连通块的个数。

## 3. 通用代码模板（C++）

```cpp
#include<bits/stdc++.h>
#define endl "\n"
using namespace std;

// 【全局区】方便 DFS 直接调用
int n, m;
vector<string> grid;

// 方向偏移数组（4连通或8连通，看题目要求）
int dx[8] = {-1, -1, -1, 0, 0, 1, 1, 1};
int dy[8] = {-1, 0, 1, -1, 1, -1, 0, 1};

// 【DFS 淹没函数】
void dfs(int x, int y)
{
    grid[x][y] = '.'; // 【核心】淹没（改成陆地），防止重复搜索

    for (int i = 0; i < 方向个数; i++) // 4连通就写 i<4，8连通写 i<8
    {
        int nx = x + dx[i];
        int ny = y + dy[i];

        // 1. 判断越界
        if (nx < 0 || nx >= n || ny < 0 || ny >= m) continue;

        // 2. 如果邻居是目标块（如水），继续搜索
        if (grid[nx][ny] == 'W')
        {
            dfs(nx, ny);
        }
    }
}

void solve()
{
    cin >> n >> m;
  
    // 读入地图
    grid.resize(n);
    for (int i = 0; i < n; i++) cin >> grid[i];

    ll ans = 0;
    // 普查全图
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            if (grid[i][j] == 'W') // 遇到目标块
            {
                ans++;
                dfs(i, j);         // 淹没这一整片连通区域
            }
        }
    }

    cout << ans << endl;
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    solve();
    return 0;
}
```

## 4. 方向数组处理（重点必背）

| 连通类型                    | 方向数 | `dx` 和 `dy` 数组定义                                                             |
| :-------------------------- | :----- | :------------------------------------------------------------------------------------ |
| **4连通**（上下左右） | `4`  | `int dx[4] = {-1, 0, 1, 0}; int dy[4] = {0, 1, 0, -1};`                             |
| **8连通**（含斜对角） | `8`  | `int dx[8] = {-1, -1, -1, 0, 0, 1, 1, 1}; int dy[8] = {-1, 0, 1, -1, 1, -1, 0, 1};` |

*（小技巧：不管几连通，循环里的越界判断和递归逻辑根本不需要改，只需要改这两个数组的数值和循环次数即可通用！）*

## 5. 关键要点与防坑指南

1. **巧妙避开 `visited` 数组**：
   如果是新手，很容易去开一个 `bool visited[105][105]` 做额外标记。但在这个模板里，**直接修改原地图 `grid[x][y] = '.'`（水淹法）** 极其优雅，既做了标记，又省了空间，这是最标准的竞赛写法。
2. **越界判断必须严格**：
   你曾经吃过亏的细节：地图行数是 `n`，下标只能是 `0` 到 `n-1`。判断越界必须写 `if (nx >= n || ny >= m)`，绝对不能写成 `if (nx > n)`。
3. **小心递归栈溢出（RE 陷阱）**：
   如果题目给了一张巨大的图（比如 \(N \times M \ge 1000 \times 1000\)），且全图都是连通的，DFS 递归层级会非常深，可能导致 **RE（栈内存溢出）**。

   * **解决方案**：如果图非常大，且题目没有禁用 BFS，最好使用 `queue` 写 **BFS（广度优先搜索）** 来替代 DFS，BFS 不会出现栈溢出问题。
4. **变量作用域**：
   **强烈建议把 `grid`、`n`、`m` 和方向数组定义在全局（`main` 和 `solve` 外面）**。这样 `dfs` 可以直接访问，免去了 C++ 传参时的繁琐和复杂度。这是图论搜索题公认的最优实践。

## 6. 进阶思考：DFS vs BFS

- **DFS（深度优先搜索，你刚用的）**：**优点**是代码极短，递归思维直观。**缺点**是遇到超大连通块会爆栈。
- **BFS（广度优先搜索）**：如果遇到大图的连通块题，改用 `queue` 去实现。BFS 不需要递归，绝对安全。
  *(等到你以后系统学习了队列和 BFS 后，你会发现这两者的判断逻辑完全一样，只需把 `递归调用` 改成 `队列 push`，就能立刻把 DFS 题变成 BFS 题。)*

**一句话总结这类题的终极心态：**
只要看到“格子地图”、“判断连通”、“统计块数”，第一步反射就是“建立方向数组”+“淹没填平法”+“双重循环普查”。以后遇到类似题，直接抄这个模板，你就能轻松拿捏！😄
