### 全局替换类问题 —— 通用解题思维笔记

**核心思想**：  
当一个操作会**同时改变某个字符的所有出现**时，该字符在最终结果中只能有一种形态（值）。因此，我们必须检查原串中每个字符的“身份”是否在所有位置上与目标串的要求保持**一致**。

---

**适用场景**（不限于交替二进制串）：
- 任何要求通过“全局替换”生成特定模式串的题目。
- 模式串可以是：交替串、全相同串、回文串、特定周期串等。
- 只要操作是“选择一个字符，把所有该字符变成另一个字符”，都可用此思想。

---

### 通用处理步骤

1. **枚举所有可能的目标模式**  
   目标串可能不止一种（如交替串有两种开头）。先确定所有候选模式。

2. **建立映射表**  
   用一个数组 `mp[字符集大小]`，初始为特殊值（如 `-1`），表示该字符尚未被分配目标值。

3. **遍历原串，检查一致性**  
   对于每个位置 `i`：
   - 计算该位置在目标模式中需要的值 `need`。
   - 获取当前字符 `ch`。
   - 若 `mp[ch]` 未分配 → 分配为 `need`。
   - 若 `mp[ch]` 已分配但与 `need` 不同 → **冲突**，当前模式不可行。
   - 若相同 → 继续。

4. **判定结果**  
   只要存在至少一种模式没有冲突，即可行。

---

**为什么这样做能覆盖所有情况？**  
- 全局替换意味着每个字符的“映射”是固定的，不能因位置不同而改变。
- 因此，只要字符在任意两个位置被要求变成不同的值，就直接不可能。
- 这种方法将问题转化为**集合映射的一致性检测**，与具体模式无关。

---

**可扩展变种**：
- 目标串是**全0或全1**：`need = 0`（或 `1`）对所有 `i`，其余逻辑不变。
- 目标串是**回文串**：`need = 目标串中对称位置的值`，检查字符映射是否在对称位置一致。
- 目标串是**特定周期串**（如 `abcabc...`）：`need = 周期模板中对应字符`，检查映射。
- 允许多种操作（如也可以全局替换成其他字母）：可扩展映射表的值域。

---

**注意事项**：
- 字符集大小需根据题目确定（小写字母 26，大写 26，数字 10，ASCII 128 等）。
- 如果目标模式数量很多，可能需要更高效的方式而非枚举所有（但通常模式数很小）。