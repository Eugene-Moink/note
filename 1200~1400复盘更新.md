
# Longest Strike

**题目**：找连续整数区间 `[l, r]`，使区间内每个整数在数组中出现 ≥ k 次，输出使 `r-l` 最大的 `l, r`。

**算法知识**：哈希表计数 + 排序 + 线性扫描

**核心卡点（精炼）**：

- **统计**：`unordered_map` 被 CF 数据卡成 O(n²) → TLE → 换 `map` 或排序统计
- **筛选**：漏掉“只保留频率 ≥ k 的数字”这步 → 直接扫原数组 → 无效比较
- **扫描**：
  - 输出格式错：把 `r-l`（长度）当成答案，题目要的是端点 `l r`
  - 初始化错：`l` / `best_l` 写了 `0`，数字范围没有 `0` → 应初始化为 `check[0]`
  - 断点重置错：`len = 0`，但当前数字是新段第一个元素，应为 `len = 1`

**一句话修正**：用 `map` 统计，筛出合法数放入 `check`，排序后扫描找最长连续段，输出时看准题目要端点还是长度。



# Scuza

**题目**：给定每级台阶的高度 `a_i`（相对前一级的增量），对于每个询问 `k`，求腿长 `k` 时能爬到的最大高度（需连续爬，遇到高度差 > k 的台阶即停）。

**算法知识**：前缀和 + 前缀最大值 + 二分查找

**核心卡点（精炼）**：

- **预处理**：构造 `mx[i] = max(a[1..i])`，表示爬到第 `i` 级所需的最小腿长；构造 `pref[i] = sum(a[1..i])`，表示爬到第 `i` 级的总高度。
- **二分查找**：对于询问 `k`，在 `mx` 上二分，用 `upper_bound` 找到第一个 `> k` 的位置 `idx`，则 `pref[idx]` 即为答案（因为能爬的台阶数就是 `idx`，对应前缀和正好是 `pref[idx]`）。
- **输出格式**：每个测试用例的 `q` 个答案用空格隔开，末尾换行（否则多个测试用例输出会粘在一起）。
- **注意**：不能用 `lower_bound`（会找第一个 `>= k`，导致少算最后一个可爬台阶），必须用 `upper_bound`；答案可能超过 int，用 `long long`。

**一句话修正**：预处理 `mx` 和 `pref`，用 `upper_bound` 在 `mx` 上查找，输出 `pref[idx]`，注意每行末尾换行。
