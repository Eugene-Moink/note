# 二维前缀和（2D Prefix Sum）通解笔记

## 适用场景
- 给定一个二维矩阵，需要频繁、多次地查询任意矩形区域内的元素总和。
- 单次查询时间复杂度从 \(O(N^2)\) 优化至 \(O(1)\)。
- 常用于二维差分、子矩阵极值计算、二维滑动窗口等问题的预处理基础。

## 核心通解模板（读取与预处理同步）

```cpp
#include<bits/stdc++.h>
#define endl "\n"
using ll = long long;
using namespace std;

void moink()
{
    ll n, m;
    cin >> n >> m;

    // 1. 只需声明一个前缀和数组，多开一行一列防越界
    vector<vector<ll>> sum(n + 1, vector<ll>(m + 1, 0));

    // 2. 边读入，边同步构建二维前缀和矩阵（不需要单独开原数组 a）
    for (ll i = 1; i <= n; i++) {
        for (ll j = 1; j <= m; j++) {
            ll val;
            cin >> val; // 每读入一个数，立刻参与计算
            sum[i][j] = sum[i - 1][j] + sum[i][j - 1] - sum[i - 1][j - 1] + val;
        }
    }

    // =============================================================
    // 3. 通解查询公式（适用于所有二维子矩阵和问题）
    // 若需要求：左上角为 (x1, y1)，右下角为 (x2, y2) 的矩形区域和
    /*
        ll sub_sum = sum[x2][y2] 
                   - sum[x1 - 1][y2] 
                   - sum[x2][y1 - 1] 
                   + sum[x1 - 1][y1 - 1];
    */
    // =============================================================

    // 4. 根据具体题目的要求，对预处理好的 sum 数组进行运算和输出
    for (ll i = 1; i <= n; i++) {
        for (ll j = 1; j <= m; j++) {
            // 此处编写本题的具体输出逻辑（如下面这行就是求平均值/比例）
            // cout << sum[i][j] / (i * j) << " ";
        }
        cout << endl;
    }
}

int main()
{
    moink();
    return 0;
}
```

## 关键注意事项（避坑指南）

- **🚀 空间优化奥秘**：同步读入计算的好处在于，**你不再需要为了存原矩阵而开辟一个 `a[n+1][m+1]` 的数组**，直接在读取本地变量 `val` 后就能更新 `sum` 数组。这在矩阵数据量极大时（如 \(10^4 \times 10^4\)）能节约几 GB 的内存空间，是非常实用的工程写法。
- **⚠️ 下标从 1 开始定律（防 RE 的命脉）**：即便你合并了读写，`i` 和 `j` 的遍历**必须**从 `1` 开始，且 `vector` 申请空间时**必须**是 `(n+1, m+1)`。如果 `i=0` 会访问 `sum[-1]`，导致严重的 **Runtime Error**。
- **⚠️ 防溢出陷阱**：矩阵叠加时，数据极易超过 `int` 上限。**请务必保证 `sum` 数组的类型和读取的 `val` 都使用 `long long`（即 `ll`）定义**。
- **⚠️ 边界必须带等号**：外层和内层的循环判断条件必须严格写死为 **`j <= m`** 和 **`i <= n`**。若漏写 `<` 变成 `j < m`，最后一列的数据将永远不会被扫描，答案必错。