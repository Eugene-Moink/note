# 最少删除使数组满足条件 —— 解题笔记

---

## 一、题目特征识别

- 给你一个数组（或字符串）
- 可以删除**任意位置**的元素（不要求连续）
- 目标：使剩下的数组满足某种性质（有序、回文、去重等）
- 求**最少删除数量**

**核心等价关系**：
> 最少删除数量 = 总长度 - 最长保留子序列长度

---

## 二、关键判断：子序列 ≠ 子数组

| 类型 | 定义 | 典型关键词 |
|------|------|-----------|
| **子数组** | 必须连续 | “连续子段”“一段”“删除连续” |
| **子序列** | 可以不连续 | “任意位置”“删除一些元素”“保留顺序” |

**本题**：删除**任意**元素 → 子序列问题

---

## 三、常见变体与对应算法

| 目标保留性质 | 求什么 | 核心算法 | 关键函数 |
|-------------|--------|----------|----------|
| 严格递增 (\(a_i < a_{i+1}\)) | 最长上升子序列 (LIS) | 贪心 + 二分 | `lower_bound` |
| 非递减 (\(a_i \le a_{i+1}\)) | 最长非递减子序列 (LNDS) | 贪心 + 二分 | `upper_bound` |
| 回文 | 最长回文子序列 (LPS) | 区间 DP | `dp[i][j]` |
| 相邻差 ≤ k | 最长连续合法段 | 双指针扫描 | 无需二分 |
| 去重保持原序 | 最长无重复子序列 | 滑动窗口 | 哈希表 |

---

## 四、LIS / LNDS 通用模板

```cpp
vector<int> tail;   // tail[i] = 长度为 i+1 的子序列的最小末尾值

for (int x : a) {
    // 根据题目选 lower_bound 或 upper_bound
    auto it = 函数(tail.begin(), tail.end(), x);

    if (it == tail.end())
        tail.push_back(x);   // 可以扩展最长长度
    else
        *it = x;             // 替换，让末尾更小，有利于后续扩展
}

int retain_len = tail.size();
int delete_count = n - retain_len;
```

---

## 五、`lower_bound` vs `upper_bound`

| 函数 | 找什么位置 | 适用场景 |
|------|-----------|----------|
| `lower_bound` | 第一个 **≥ x** 的位置 | **严格递增**（不允许相等连续） |
| `upper_bound` | 第一个 **> x** 的位置 | **非递减**（允许相等连续） |

**记忆口诀**：
- 严格递增 → 不容等于 → `lower_bound`（找 >=）
- 非递减 → 允许等于 → `upper_bound`（找 >）

---

## 六、为什么不能统计连续下降段？

**常见错误**：统计 `a[i] < a[i-1]` 连续出现的次数，然后输出 `n - 该长度`。

**错误原因**：
- 连续下降段统计的是**连续**的相邻下降
- 但 LNDS 允许**跳过中间元素**，不要求连续

**反例**：`[5, 4, 3, 2, 1]`
- 最长非递减子序列长度 = 1（任意单个元素）
- 连续下降段长度 = 5
- 统计连续下降段会输出 `5-5=0`，正确答案是 `5-1=4`

---

## 七、解题流程（五步走）

```
1. 判断能否不连续
   ├── 能跳过元素 → 子序列问题 → 进入第2步
   └── 必须连续 → 子数组问题 → 用双指针/滑动窗口

2. 明确保留性质
   ├── 递增/非递增
   ├── 回文
   ├── 去重
   └── 相邻差≤k

3. 选择对应算法
   ├── LIS/LNDS → 贪心+二分
   ├── LPS → 区间DP
   ├── 双指针扫描 → 滑动窗口
   └── 哈希表 → 记录位置

4. 套模板计算“最长保留长度”

5. 输出 n - 最长保留长度
```

---

## 八、常见易错点检查表

| 易错点 | 正确做法 |
|--------|----------|
| 把子序列当成子数组 | 记住：子序列可以不连续 |
| 递增/非递减搞混 | 有等于号 = 非递减，用 `upper_bound` |
| 输出长度而不是删除数 | 必须输出 `n - tail.size()` |
| 用原数组长度代替 `tail.size()` | 不能直接用下标，`tail.size()` 才是 LNDS 长度 |
| 末尾值用原值存 | 必须用 `*it = x` 更新为更小的末尾值 |

---

## 九、一句话总结

> **“最少删除 = n - 最长保留，保留性质决定算法：不连续用 LIS/LNDS/LPS，连续用双指针。”**

遇到这类题时，先问自己两个问题：
1. 删除后剩下的元素必须**连续**还是可以**不连续**？
2. 要求剩下的数组满足什么**性质**？

回答完这两个问题，算法就确定了。