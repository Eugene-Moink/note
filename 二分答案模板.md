# 二分答案与二分查找

## 一、适用场景

- **基础二分查找**：在**有序**序列中快速查找某个值（时间复杂度 \(O(\log N)\)）。
- **二分答案**：求**最大值中的最小值** 或 **最小值中的最大值**，**答案存在单调性**，且能够写一个 \(O(N)\) 的 `check(mid)` 函数来快速验证当前答案是否合法。

## 二、核心模板

### 1. 基础二分查找（在有序数组中找指定值）

```cpp
ll l = 0, r = n - 1;
ll target;
while (l <= r) 
{
    ll mid = l + (r - l) / 2;
    if (a[mid] == target) 
    { 
        // 找到目标值
        break; 
    } 
    else if (a[mid] < target) 
    {
        l = mid + 1;
    } 
    else 
    {
        r = mid - 1;
    }
}
```

### 2. 二分答案 —— 求满足条件的**最大值**（如“砍树”求锯片最高高度）

```cpp
ll l = 0, r = max_possible; // r 为答案的上限（如树的最大高度）
ll ans = 0;

while (l <= r) 
{
    ll mid = l + (r - l) / 2; // 取中点，避免 l+r 溢出

    if (check(mid))           // check 检查该 mid 高度能否砍够木材
    { 
        ans = mid;            // 如果合法，记录当前的可行性答案
        l = mid + 1;          // 尝试往更大的方向搜索
    } 
    else 
    {
        r = mid - 1;          // 不合法，说明高度太高，往低处搜索
    }
}
```

### 3. 二分答案 —— 求满足条件的**最小值**（如“分巧克力”求能切出的最大边长）

```cpp
ll l = 1, r = max_possible; // 一般最小值下限为 1
ll ans = 0;

while (l <= r) 
{
    ll mid = l + (r - l) / 2;

    if (check(mid))           // check 检查边长为 mid 时，能否切出至少 k 块
    { 
        ans = mid;            // 记录可行解
        r = mid - 1;          // 尝试往更小的方向搜索（找最小值）
    } 
    else 
    {
        l = mid + 1;          // 不合法，说明太小，往大搜索
    }
}
```

## 三、关键注意事项（防坑必读）

### 1. `mid` 防溢出写法

```cpp
// ❌ 错误写法（l + r 可能超过 long long 范围）
ll mid = (l + r) / 2; 

// ✅ 正确写法（安全防溢出）
ll mid = l + (r - l) / 2;
```

### 2. 左右边界 `+1` 和 `-1` 的原理

- 二分时，一旦检验过 `mid`，就需要把它从下一轮搜索范围中**彻底排除**，防止陷入死循环。
- 因此必须写 `l = mid + 1;` 或 `r = mid - 1;`。

### 3. 数据范围与 `long long`

- 只要题目中涉及**累加、求和、或者数值范围超过 \(10^5\)**，`mid`、`ans`、`check` 里的累加变量**必须全部用 `long long`**。

### 4. 答案的初始化

- 求最大值时，`ans` 初始化为 `0`（或比合法解更小的数）。
- 求最小值时，`ans` 初始化为 `max_possible`（或比合法解更大的数）。

### 5. 实数二分（带小数的题，如解方程）

如果题目要求结果精确到 `1e-6`，二分条件不是 `l <= r`，而是：

```cpp
while (r - l > 1e-7) // 比要求精度更小一些
{
    double mid = (l + r) / 2;
    if (check(mid)) l = mid;
    else r = mid;
}
```

## 四、速记口诀

> **“答案二分的核心，不是找数，是卡范围。
> 先写 `check` 判合法性，再调边界找极值。
> 最大值往上抬（`l = mid + 1`），最小值往下压（`r = mid - 1`）**

# 二分答案模板笔记

## 适用场景

- **最值优化问题**：求最大值中的最小值、最小值中的最大值
- **存在单调性**：若 x 满足条件，则 x±1 也存在单调关系
- **可验证性**：给定值能在合理时间内验证是否符合条件

## 核心模板

```cpp
// 求满足条件的最大值
ll left = 最小可能值, right = 最大可能值;
ll ans = 0;

while (left <= right) {
    ll mid = left + (right - left) / 2;
  
    if (check(mid)) {           // 检查mid是否满足条件
        ans = mid;              // 记录可行解
        left = mid + 1;         // 继续寻找更大值
    } else {
        right = mid - 1;        // 当前值不满足，向小值搜索
    }
}

// 求满足条件的最小值
ll left = 最小可能值, right = 最大可能值;
ll ans = 最大可能值;

while (left <= right) {
    ll mid = left + (right - left) / 2;
  
    if (check(mid)) {
        ans = mid;              // 记录可行解
        right = mid - 1;        // 继续寻找更小值
    } else {
        left = mid + 1;         // 当前值不满足，向大值搜索
    }
}
```

## 关键注意事项

### 1. 防溢出技巧

```cpp
// 错误：可能溢出
if (mid * mid * mid <= n)

// 正确：防止溢出
if (mid <= n / mid / mid)
```

### 2. 搜索边界

- `left = mid + 1` vs `left = mid`
- `right = mid - 1` vs `right = mid`
- 关键在于 mid 是否可能是答案

### 3. 初始化策略

- `ans` 初始值应为"无效值"
- 搜索范围要覆盖所有可能性

## 快速判断方法

遇到问题时问自己：

1. **是否求最值？** → 最大值/最小值
2. **是否存在单调性？** → x满足→x±1的规律性
3. **能否快速验证？** → check函数效率

全部是 → 使用二分答案！

## 典型变体

- **最大化最小值**：最小值尽可能大
- **最小化最大值**：最大值尽可能小
- **第k大/小**：在有序序列中定位

记住：**单调性 + 验证函数 + 二分搜索 = 二分答案**
