# 栈（Stack）—— 动态序列与撤销操作

## 一、适用场景

- **后进先出（LIFO）模拟**：需要动态维护一个序列，且操作只发生在序列末尾（如撤销、回退、退格）。
- **括号匹配问题**：判断括号序列是否合法，或求最长有效括号子串。
- **表达式求值**：中缀转后缀、计算器实现。
- **单调栈**：寻找数组中下一个更大/更小元素（冲国二/银牌必备进阶）。

**核心信号词（看到马上想到栈）**：
> “删除末尾”、“撤销上一步”、“匹配配对”、“历史记录”、“回退”、“最近操作”。

## 二、核心模板

### 1. 标准库栈（适用于纯逻辑操作，无需输出最终栈内元素）

```cpp
stack<char> st;
for (char c : s) 
{
    if (c == 'D') 
    {
        if (!st.empty())    // 必须判空！
        {
            st.pop();
        }
    } 
    else 
    {
        st.push(c);
    }
}
```

### 2. 字符串模拟栈（竞赛首选 —— 最终需要输出栈内所有元素时使用）

```cpp
string ans;                 // 栈顶在字符串末尾
for (char c : s) 
{
    if (c == 'D') 
    {
        if (!ans.empty())   // 防越界
        {
            ans.pop_back(); // 弹出栈顶（末尾）
        }
    } 
    else 
    {
        ans.push_back(c);   // 压入栈顶（末尾）
    }
}
// 最后直接输出 ans（若为空需特判）
```

### 3. 括号匹配（自检模板）

```cpp
stack<char> st;
bool ok = true;
for (char c : s) 
{
    if (c == '(' || c == '[' || c == '{') 
    {
        st.push(c);
    } 
    else 
    {
        if (st.empty()) { ok = false; break; }
        char top = st.top();
        if ((c == ')' && top != '(') ||
            (c == ']' && top != '[') ||
            (c == '}' && top != '{')) 
        {
            ok = false; break;
        }
        st.pop();
    }
}
if (!st.empty()) ok = false; // 左括号多了也不行
```

## 三、关键注意事项（防坑必读）

### 1. 弹出前必须判空（Red Flag 🚩）

```cpp
// ❌ 致命错误：当栈为空时直接 pop 会导致运行时错误（RE）
st.pop(); 

// ✅ 安全写法
if (!st.empty()) 
{
    st.pop();
}
```

### 2. 最终序列为空的特殊输出

- 很多题目（如伐楚题）要求：如果最终序列为空，输出特定的占位符（如 `!!!!` 或 `NULL`）。
- **错误习惯**：`cout << ans << endl;` 只会输出一个空行，不符合题目要求。
- **正确姿势**：

```cpp
if (ans.empty()) cout << "!!!!" << endl;
else cout << ans << endl;
```

### 3. 遍历时使用 `for(char c : s)` 更安全

- 如果使用 `for (int i = 0; i < s.size(); i++)`，注意 `s.size()` 返回 `size_t` 无符号类型，和 `int` 比较时编译器可能报警告。
- 推荐用范围 for 循环，简洁且不会出错。

### 4. 用 `string` 模拟栈比 `stack` 更快

- `stack` 默认底层是 `deque`，内存开销略大。
- `string` 的 `push_back` / `pop_back` 是连续内存，**缓存友好**，且最后直接输出无需额外转换。在 1e6 数据量下，`string` 稳胜。

## 四、典型变体（思维拔高）

- **变体 A**：删除键改为 `Backspace`，删除当前序列中的**第一个字符**（队列，FIFO）→ 此时需要改用 `deque` 或 `queue` + 双指针。
- **变体 B**：删除操作可撤销（即 `Ctrl+Z`）→ 需要**双栈**（一个存操作历史，一个存撤销历史）。
- **变体 C**：单调栈求下一更大元素（维护单调递减栈，新元素入栈时弹出比它小的栈顶）。

## 五、速记口诀

> **“后进先出是栈魂，撤销回退靠它存。**
> **压入末尾弹出尾，字符串来当栈身。**
> **弹出之前必判空，否则 RE 找上门。**
> **答案若空需特判，占位符号要区分。”**

