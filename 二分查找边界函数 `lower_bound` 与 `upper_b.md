# 模板笔记：二分查找边界函数 `lower_bound` 与 `upper_bound`

## 1. 前置条件
**这两个函数只能用于“已经有序”的数组或 `vector`。** （通常是从小到大排序）

## 2. 函数作用与返回值

### 🔍 `lower_bound(起始迭代器, 结束迭代器, 目标值)`
*   **作用**：查找**第一个“大于等于”**目标值的位置。
*   **返回值**：返回一个**迭代器**。如果找到，指向该元素；如果所有元素都小于目标值，则指向数组末尾（`arr.end()`）。

### 🔼 `upper_bound(起始迭代器, 结束迭代器, 目标值)`
*   **作用**：查找**第一个“严格大于”**目标值的位置。
*   **返回值**：返回一个**迭代器**。如果所有元素都小于等于目标值，则指向数组末尾（`arr.end()`）。

---

## 3. 如何将迭代器转换为数组下标（0-based）
因为这两个函数返回的是 `迭代器`，而题目通常要输出 `下标`（从 0 开始），转换公式极其简单：
```cpp
// 假设数组叫 a
auto it = lower_bound(a.begin(), a.end(), x);
ll idx = it - a.begin(); // 【核心转换】迭代器相减，就是下标
```

---

## 4. 查找 `X` 出现位置的通用逻辑模板

### ✅ 查找 `X` 的【第一次出现】（对应题目中的 `L` 操作）
```cpp
ll idx = lower_bound(a.begin(), a.end(), x) - a.begin();

// 检查是否越界，或者找到的元素根本不是 x（说明 x 不存在）
if (idx == n || a[idx] != x) {
    cout << -1 << endl;
} else {
    cout << idx << endl;
}
```

### ✅ 查找 `X` 的【最后一次出现】（对应题目中的 `R` 操作）
因为 `upper_bound` 找到的是“第一个比 X 大的位置”，这个位置往前推 1 位，就是 X 最后出现的地方！
```cpp
ll idx = upper_bound(a.begin(), a.end(), x) - a.begin();

// 如果 idx==0，说明数组第一个元素就大于 x，x 肯定不存在
// 并且要检查 idx-1 那个位置的值是不是 x
if (idx == 0 || a[idx - 1] != x) {
    cout << -1 << endl;
} else {
    cout << idx - 1 << endl; // 注意这里减 1！
}
```

---

## 5. ⚠️ 核心防坑指南（竞赛中极易翻车的点）

### 🚨 陷阱 1：`upper_bound` 的边界检查（你刚做对的地方！）
当 `upper_bound` 返回 `0` 时，千万不能写成 `a[idx] != x`，否则会越界读到 `a[-1]`。**必须写成 `idx == 0 || a[idx-1] != x`**（利用 `||` 的短路特性，先判断 `idx==0`）。

### 🚨 陷阱 2：全局 `vector` 千万不要预初始化！
**错误写法（导致 RE）：**
```cpp
ll n, m;
vector<ll> arr(n); // 【致命错误】此时 n 还没读入，是 0！
```
**正确写法（必须遵循）：**
```cpp
ll n, m;
vector<ll> arr; // 1. 只声明，不分配空间

int main() {
    cin >> n >> m;
    arr.resize(n); // 2. 读入 n 后，立刻 resize 分配空间
}
```

### 🚨 陷阱 3：确信 `lower_bound` 找到的是不是 `x`？
`lower_bound` 只是找到了“第一个大于等于 X 的位置”。如果数组里根本没有 `X`，它会找到一个比 X 大的数。**一定要用 `if (arr[idx] != x)` 判断一下，否则会找到错误的数！**

### 💡 进阶记忆口诀
> **找左边界用 `lower`，找右边界用 `upper`，迭代器减开头算下标，边界越界防暴毙。**