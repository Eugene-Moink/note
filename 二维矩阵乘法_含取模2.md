# 模板笔记：二维矩阵乘法（含取模 2）

## 1. 运算核心原理
矩阵乘法的基本口诀是：**“左矩阵的每一行，乘以右矩阵的每一列，对应位置相乘再相加。”**
设有两个 \(m \times m\) 的矩阵 \(A\) 和 \(B\)，结果矩阵 \(C\) 的每一个元素计算公式为：
\[
C_{i,j} = \sum_{k=0}^{m-1} (A_{i,k} \times B_{k,j}) \pmod 2
\]

---

## 2. C++ 三重循环实现（核心模板）
```cpp
vector<vector<ll>> A(m, vector<ll>(m));
vector<vector<ll>> B(m, vector<ll>(m));
vector<vector<ll>> C(m, vector<ll>(m, 0));

for (int i = 0; i < m; i++) {           // 遍历结果矩阵的行
    for (int j = 0; j < m; j++) {       // 遍历结果矩阵的列
        C[i][j] = 0;                    // 计算前先清零当前格
        for (int k = 0; k < m; k++) {   // k 是桥梁：A 的第 i 行 × B 的第 j 列
            C[i][j] = (C[i][j] + A[i][k] * B[k][j]) % 2;
        }
    }
}
```

---

## 3. 模 2 运算与特殊约束
- **为什么必须 `% 2`**：题目通常规定矩阵元素只能为 `0` 或 `1`（布尔矩阵）。
- 如果在累加时不取模，当矩阵边长 \(m\) 较大（如 50）且元素全为 1 时，单个格子累加和会超过 `long long` 的安全范围或与题目要求的 01 格式不符。
- 累加时 `(结果 + 项) % 2` 与累加完再取模效果完全相同，但边加边模能有效防止中间结果过大。

---

## 4. 直观类比（“消消乐”模型）
可以将参与矩阵乘法的两个矩阵看作：
1. **旧矩阵（结果累加器）**：当前已经融合好的矩阵 `ans`。
2. **新矩阵**：刚刚读入的需要融合进来的矩阵 `cur`。
3. **动作**：让旧矩阵的**每一行**，去“扫描”新矩阵的**每一列**。对应位置碰一碰（相乘），加起来是奇数就是 `1`，偶数就是 `0`。
4. **更新**：算完之后，必须用最新的结果覆盖旧矩阵（`ans = C`），留着跟下一个矩阵继续碰。

---

## 5. 完整 AC 代码模板（带连续融合）
```cpp
#include<bits/stdc++.h>
#define endl "\n"
using ll = long long;
using namespace std;

void moink()
{
    ll n, m;
    cin >> n >> m;

    vector<vector<ll>> ans(m, vector<ll>(m));
    vector<vector<ll>> cur(m, vector<ll>(m));
    vector<vector<ll>> tmp(m, vector<ll>(m, 0));

    // 读入第一个矩阵作为初始结果
    for (ll i = 0; i < m; i++)
        for (ll j = 0; j < m; j++)
            cin >> ans[i][j];

    // 依次读入剩下的矩阵进行融合
    for (ll idx = 1; idx < n; idx++)
    {
        // 1. 读入当前新矩阵
        for (ll i = 0; i < m; i++)
            for (ll j = 0; j < m; j++)
                cin >> cur[i][j];

        // 2. 矩阵乘法：tmp = ans * cur （消消乐）
        for (ll i = 0; i < m; i++)
        {
            for (ll j = 0; j < m; j++)
            {
                tmp[i][j] = 0;
                for (ll k = 0; k < m; k++)
                {
                    tmp[i][j] = (tmp[i][j] + ans[i][k] * cur[k][j]) % 2;
                }
            }
        }

        // 3. 将融合结果更新回 ans，准备下一轮
        ans = tmp;
    }

    // 输出最终矩阵
    for (ll i = 0; i < m; i++)
    {
        for (ll j = 0; j < m; j++)
        {
            cout << ans[i][j];
            if (j < m - 1) cout << " ";
        }
        cout << endl;
    }
}
```