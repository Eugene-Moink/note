### 模板笔记：寻找满足特定数字特征的最小倍数

#### 1. 通用代码模板（多组测试数据）
```cpp
#include<bits/stdc++.h>
#define endl "\n"
using ll = long long;
using namespace std;

// 检查 n 是否满足题目要求的数字特征
bool check(ll n)
{
    string s = to_string(n);
    // 示例：检查是否包含连续两个 '0'
    if (s.find("00") != string::npos)
    {
        return true;
    }
    return false;
}

int main()
{
    ll t;
    cin >> t;
    while (t--)
    {
        ll k;
        cin >> k;

        // 枚举倍数，通常 1~100 次循环内必有解（例如 K*100 必合法）
        for (ll i = 1; i <= 100; i++)
        {
            ll n = k * i;
            if (check(n))
            {
                cout << n << endl;
                break; // 找到最小的倍数后立即退出循环
            }
        }
    }
    return 0;
}
```

#### 2. 核心易错点
- **数据类型**：`n = k * i` 必须用 `long long` 接收，防止乘积溢出 32 位整数（样例可达 169 亿）。
- **分号陷阱**：`if (条件) ;` 末尾的分号会导致条件成立时执行空语句，务必改成 `if (条件) { 执行语句; }`。
- **终止循环**：一旦 `check(n)` 为真，必须立即 `break;`，否则会继续枚举并输出更大的错误答案。

#### 3. check() 函数变形替换
*(根据题目具体要求，替换 `check` 函数内部逻辑)*

- **要求数字不含某个字符（如不含 '7'）**：
  ```cpp
  if (s.find('7') == string::npos) return true;
  ```

- **要求各位数字之和为特定值（如等于 10）**：
  ```cpp
  ll sum = 0, temp = n;
  while (temp > 0) { sum += temp % 10; temp /= 10; }
  if (sum == 10) return true;
  ```

- **要求数字是回文数**：
  ```cpp
  string s = to_string(n);
  string rev = s;
  reverse(rev.begin(), rev.end());
  if (s == rev) return true;
  ```