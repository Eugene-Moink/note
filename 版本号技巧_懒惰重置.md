### 版本号技巧（懒惰重置）

**关键词**：`数组重置`、`还原初始值`、`全局清零`、`时间戳`

**适用场景**：有一个数组，需要多次对某个位置进行加法/修改，但有时会触发“全局重置”（所有元素变回初始值）。每次重置后，后续操作在新状态下继续进行。

**核心思想**：不真正遍历数组重置，而是用一个全局 `ver`（版本号）来标记“当前是第几轮”。每个位置记录它最后一次被修改是在哪个版本号。查询当前位置的值时，如果它的版本号小于全局版本号，说明它已过期，应返回初始值；否则返回修改后的值。

**实现模板**：

```cpp
// 初始数组 a[0..n-1]，永不修改
vector<ll> a(n);
for (int i = 0; i < n; i++) cin >> a[i];

// 存储修改后的值，仅当 last_ver[i] == ver 时有效
vector<ll> val(n);
// 每个位置最后一次被修改的版本号，初始设为 -1（或任何小于 0 的数）
vector<int> last_ver(n, -1);
int ver = 0;  // 全局版本号

while (操作次数--) {
    int pos; ll c;
    cin >> pos >> c;
    pos--;  // 转为 0-based

    // 获取当前位置的当前值
    ll cur = (last_ver[pos] < ver) ? a[pos] : val[pos];

    // 计算新值
    ll new_val = cur + c;

    if (new_val > 阈值) {
        ver++;  // 触发全局重置：版本号 +1，之前所有修改自动失效
        // 注意：不修改任何 val 或 last_ver，旧数据自然过期
    } else {
        last_ver[pos] = ver;  // 标记这个位置在最新版本被修改过
        val[pos] = new_val;   // 存储新值
    }
}

// 输出最终结果
for (int i = 0; i < n; i++) {
    ll ans = (last_ver[i] < ver) ? a[i] : val[i];
    cout << ans << " \n"[i == n - 1];
}
```

**为什么这样是对的**：
- 每次重置时，只是 `ver++`，不触碰任何数组元素。
- 查询某个位置时，通过比较 `last_ver[i]` 和当前 `ver` 的大小，就能知道该位置的值是否“过期”。如果过期，就返回原始值 `a[i]`；否则返回修改后的值 `val[i]`。
- 这样每次操作（包括重置）都是 O(1) 的，总复杂度 O(n + m)。

**注意事项**：
- 需要同时维护原始数组和修改后的值数组。
- 如果初始值可能为 0 且需要频繁重置，版本号初始设为 1 更安全（让 `last_ver` 初始为 0，查询时用 `< ver` 判断）。
- 这个技巧同样适用于“全局赋值成同一个值”的场景，只需把初始值换成那个统一值即可。

---
