# 桶计数（值域计数）解题笔记

## 适用场景
- 数据规模极大（\(N \ge 10^5\)），**无法使用 \(O(N^2)\) 的两层暴力循环**。
- 题目中的元素数值范围极小（例如给定的限制 \(a_i \le 100\) 或 \(a_i \le 1000\)）。
- 需要在遍历过程中，快速得知**“当前元素之前，有多少个满足某种值域条件的元素”**（如本题：求有多少个比当前数字大的逆序对）。
- 常用于代替**树状数组**或**归并排序**，在数据范围极小时作为最高效的解法。

## 核心模板代码
```cpp
#include<bits/stdc++.h>
#define endl "\n"
using ll = long long;
using namespace std;

const ll MOD = 100; // 题目要求的取模值

void moink()
{
    ll n;
    cin >> n;
    vector<ll> arr(n);
    for (ll i = 0; i < n; i++) cin >> arr[i];

    // 1. 根据题目给出的数值最大值，开一个计数桶
    // 例如 a_i <= 100，为了保证绝对安全，多开一点点空间，开 105 或 101 均可
    vector<ll> cnts(105, 0); 
    ll ans = 0;

    // 2. 遍历所有元素（注意必须遍历到底，不能只到 n-1）
    for (ll i = 0; i < n; i++)
    {
        ll x = arr[i];
        
        // 3. 判断：在当前元素之前，有多少个满足条件的元素？
        // 这里是从 x+1 查找到最大值 100
        for (ll j = x + 1; j <= 100; j++) 
        {
            ans = (ans + cnts[j]) % MOD; // 每次累加立刻取模防溢出
        }
        
        // 4. 把当前元素存入桶中，供后续的元素去查询
        cnts[x]++;
    }

    cout << ans << endl;
}
```

## 关键注意事项（避坑指南）

- **核心思维跨越**：不要被 \(N\) 的数据规模吓到。**双循环的复杂度是 \(O(N^2)\)，桶计数的复杂度是 \(O(N \times \text{最大值})\)**。因为最大值（如 100）是极小常数，所以整体复杂度几乎等同于 \(O(N)\)，足以通过 \(N=3\times 10^5\) 的数据。
- **数组越界（RE 致命陷阱）**：定义 `vector<ll> cnts(101, 0)` 时，合法下标是 `0~100`。如果你遍历时写了 `j <= 101`，当 `x=100` 时就会访问 `cnts[101]`，直接导致程序崩溃。**规则是：桶开得比最大值大一点（如 105），内层循环直接写 `j <= 最大值` 即可（如 `j <= 100`）。**
- **必须遍历所有元素**：外层循环必须是 `for (ll i = 0; i < n; i++)`。很多新手容易习惯性地写成 `i < n - 1`，这样会导致**数组最后一个元素直接被跳过**，无法参与后续统计，答案必然偏小。
- **防溢出取模**：逆序对的数量最大可能达到 \(N \times (N-1) / 2\)，对于 \(3\times 10^5\) 的数据量，这个值（约 \(4.5 \times 10^{10}\)）**会爆掉 `long long` 的上限**。**必须养成习惯**：在内层累加时立刻执行 `(ans + cnts[j]) % MOD` 取模，不要等到最后再去取模。