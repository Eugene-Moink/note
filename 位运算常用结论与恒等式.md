# 位运算常用结论与恒等式笔记（C++ 竞赛向）

## 适用场景
- 遇到需要**两两配对并求和**的位运算题目（如 `&`、`|`、`^`），用于将 \(O(N^2)\) 降维到 \(O(N)\)。
- 状态压缩动态规划（DP）、子集枚举、集合状态维护。
- 快速判断奇偶性、是否为 \(2\) 的幂。
- 不使用乘除法指令，仅用移位提升微小性能。

## 一、核心运算恒等式（极高频化简法）
**务必熟记这 3 条，是解决“两两配对求和”时防止 TLE 的核心武器：**

1. `(x & y) + (x | y) = x + y`  *(你刚刚在魔法珠子题里用到的)*
2. `x + y = (x ^ y) + 2 * (x & y)`
3. `x ^ y = (x | y) - (x & y)`

> 💡 用法提示：在暴力双层循环求所有 `(a[i] op a[j])` 之和时，先看一眼题目是否可以用上面等式替换。如果能替换成 `a[i] + a[j]`，就可以直接提取公因式，变成 \(O(1)\) 计算。

## 二、移位运算与乘除的快速替换
- `x * 2` \(\Longleftrightarrow\) `x << 1`
- `x / 2` \(\Longleftrightarrow\) `x >> 1`（整数除法，向下取整）
- `x * 2^k` \(\Longleftrightarrow\) `x << k`
- `x / 2^k` \(\Longleftrightarrow\) `x >> k`

> ⚠️ **避坑指南**：移位只适用于**乘除 2 的整数次幂**。乘 3 时千万别写成 `x << 3`（那代表乘 8），必须写 `x * 3` 或 `(x << 1) + x`。

## 三、奇偶与 2 的幂特殊判定
- 判奇数：`if (x & 1)`（比 `x % 2 != 0` 更快，不用做除法）
- 判偶数：`if ((x & 1) == 0)`
- **判一个数是否是 2 的整数次幂（高频考点）**：
  `if ((x & (x - 1)) == 0)`
  *例如：8（1000）& 7（0111）= 0，是 2 的幂；6（110）& 5（101）= 4，不是。*

## 四、状态压缩与集合操作（DP 基础）
在 \(N \le 20\) 的数据范围下，常用一个二进制整数 `mask` 来代表选取的集合：

- **取第 k 位的状态**：`(mask >> k) & 1`
- **把第 k 位变成 1（加入集合）**：`mask | (1 << k)`
- **把第 k 位变成 0（移出集合）**：`mask & ~(1 << k)`
- **翻转第 k 位**：`mask ^ (1 << k)`
- **判断两个集合是否有交集**：`if (mask1 & mask2)`
- **合并两个集合**：`mask1 | mask2`

> ⚠️ **终极防爆提醒**：做状态压缩时，如果 `1 << k` 中的 `k` 有可能超过 `31`，**务必写成 `1LL << k`**。`1` 默认是 `int`，强行左移 40 位会直接变成 0 或者负数导致 WA，加上 `LL` 转成 `long long` 才是安全的。

## 五、GCC 内置位运算神器（直接调用，避免手写）
这些都是 GCC 编译器自带的极速内置函数，直接放在代码里即可使用，**极其省时且防错**：

- **`__builtin_popcount(x)`**：计算 `int x` 二进制中 1 的个数。
- **`__builtin_popcountll(x)`**：计算 `long long x` 二进制中 1 的个数。
- **`__builtin_ctz(x)`**：计算 `x` 的二进制**末尾连续 0** 的个数（Count Trailing Zeros）。
- **`__builtin_clz(x)`**：计算 `x` 的二进制**前导 0** 的个数（Count Leading Zeros）。