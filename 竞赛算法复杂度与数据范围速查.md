
# 📘 竞赛算法复杂度与数据范围速查全览（含 2S / 1024M 扩展及对标算法）

> **基准：**
>
> - **1S** ≈ \(1 \times 10^8\) 次运算。
> - **2S** ≈ \(2 \times 10^8\) 次运算（大常数也能救）。
> - **256M** ≈ `int` 数组 \(6 \times 10^7\) 个。
> - **1024M** ≈ `int` 数组 \(2.5 \times 10^8\) 个。

## 一、 极限搜索与状态压缩类

|   数据范围   |   基础限时   |       宽松 2S/1024M       |     复杂度     | 对应算法 / 数据结构                                           |
| :----------: | :-----------: | :-----------------------: | :------------: | :------------------------------------------------------------ |
| \(N \le 15\) | \(O(N!)\) 过 |  \(N \le 20\) 以下可剪枝  |   \(O(N!)\)   | **DFS 回溯搜索**、全排列、TSP 暴力枚举、N 皇后          |
| \(N \le 20\) | \(O(2^N)\) 过 | \(N \le 22 \sim 23\) 可过 |   \(O(2^N)\)   | **状态压缩 DP（状压 DP）**、集合覆盖、子集枚举          |
| \(N \le 40\) |   容易被卡   |    \(N \le 46\) 仍可过    | \(O(2^{N/2})\) | **Meet-in-the-Middle（折半搜索 / 双向 DFS）**、折半枚举 |

---

## 二、 中等规模多项式算法（2S/1024M 极大放宽）

|    数据范围    | 基础限时 |     宽松 2S/1024M     |   复杂度   | 对应算法 / 数据结构                                        |
| :------------: | :------: | :--------------------: | :--------: | :--------------------------------------------------------- |
| \(N \le 400\) |   稳过   |   **稳定通过**   | \(O(N^3)\) | **Floyd 全源最短路**、矩阵乘法、经典区间 DP          |
| \(N \le 500\) |   可过   |   **非常轻松**   | \(O(N^3)\) | **区间 DP（石子合并/矩阵链）**、基础线性代数运算     |
| \(N \le 2000\) |  勉强过  |    **轻松过**    | \(O(N^2)\) | **LCS 最长公共子序列**、部分搜索剪枝                 |
| \(N \le 5000\) | 1S很危险 | **2S下勉强能过** | \(O(N^2)\) | 常数极小的双层枚举、**LIS 最长递增子序列（朴素版）** |
| \(N \le 7000\) |   必死   |     2S下需极致优化     | \(O(N^2)\) | 不推荐，需转为\(O(N\log N)\) 或 \(O(N)\)                   |

> ✅ **1024M 红利**：以前 256M 不敢开的 `ll a[1005][1005]`（8MB），现在可以大胆开 **`ll a[5005][5005]`（约 200MB，1024M 下安全）**，DP 状态可以放心开更多维。

---

## 三、 高性能与常用数据结构（比赛绝对主力区）

|            数据范围            |    基础限时    |              宽松 2S/1024M              |          复杂度          | 对应算法 / 数据结构                                                                                                        |
| :-----------------------------: | :------------: | :--------------------------------------: | :-----------------------: | :------------------------------------------------------------------------------------------------------------------------- |
|         \(N \le 10^4\)         |      可过      |             **轻松过**             |     \(O(N \sqrt N)\)     | **分块算法（Sqrt Decomp）**、基础莫队算法（离线区间查询）                                                            |
|         \(N \le 10^5\)         |    常卡边界    |              **可过**              |     \(O(N \log^2 N)\)     | 树状数组/线段树套二分、CDQ 分治基础、K-D Tree                                                                              |
| **\(N \le 2\times10^5\)** | **稳过** |              **秒过**              | **\(O(N \log N)\)** | **排序 + 贪心、二分答案、Dijkstra 最短路、Kruskal 最小生成树、DSU 并查集、树状数组基础、`set/map` 操作、LCA 倍增** |
|      \(N \le 5\times10^5\)      |  1S 下常 TLE  |            **2S 可过**            |      \(O(N \log N)\)      | 注意代码常数，尽量手写快读或避免递归，**线段树/树状数组**                                                            |
|         \(N \le 10^6\)         |  仅限\(O(N)\)  | **2S 下小常数 \(O(N\log N)\)可过** |         \(O(N)\)         | **前缀和/差分、BFS/DFS 遍历、埃氏筛/线性筛、单调队列（滑动窗口）、KMP 字符串匹配、`unordered_map` 哈希**           |

---

## 四、 数论与 \(O(1)\) 快速推导类（2S 宽容度极大）

|     数据范围     |     基础限时     |          宽松 2S/1024M          |             复杂度             | 对应算法 / 数据结构                                                       |
| :---------------: | :---------------: | :------------------------------: | :-----------------------------: | :------------------------------------------------------------------------ |
|  \(N \le 10^9\)  |       稳过       |          **秒过**          | \(O(\sqrt N)\) 或 \(O(\log N)\) | **试除法质因数分解、GCD / 扩展欧几里得、快速幂、二分查找/二分答案** |
| \(N \le 10^{18}\) | 仅限\(O(\log N)\) | **\(O(\log^2 N)\) 也能过** |          \(O(\log N)\)          | **矩阵快速幂**、Miller-Rabin 素性测试、Pollard-Rho 大数分解         |

---

### 💡 实战中如何利用 2S / 1024M 挽回局面？

1. **2 秒扩容**：如果你写了一手 `O(N^2)` 的算法（例如 LCS 动态规划），发现 `N` 刚好在 `2000~3000` 左右，在 1S 的题目里可能 TLE，但在 2S 限制下大概率能直接救回来。
2. **1024M 扩容**：以前不敢在 `ll` 数组上开 `N=5000` 的双重 DP，现在开 `N=10000` 都能跑（注意总计算量 \(10^8\) 在 2S 下压力很大，但内存绝对不是瓶颈）。
3. **向量空间膨胀**：甚至可以用 `bool is_prime[100000000]`（1亿个布尔，占 100MB，256M 会爆，但 1024M 随便开）来预处理质数表，常数降到极致。

**💡 开题定心丸：**
先看 \(N\)、\(Q\) 最大值，然后对照这张表，直接选出 **“对应算法/数据结构”** 那一列作为你的起手模板，不要上来就硬想。把这张表存进你的竞赛笔记里，以后看到 \(2\times10^5\) 就知道直接上优先队列 + 二分；看到 \(10^6\) 就用前缀和 + BFS。💪
