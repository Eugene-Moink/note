### 字符串拆分/分段类问题的思维方式

这类问题的特征是：给定一个字符串，你需要将它按照某种规则切分成若干段（子串），每段的长度由该段的结尾字符或某种局部特征决定。

---

**核心思维：从后往前处理**

- **为什么？** 段的结构往往由**结尾字符**决定，而不是开头。
- 从前往后看，看到一个字符时，你无法确定它属于多长的段（因为段长取决于后面还没看到的字符）。
- 从后往前看，最后一个字符直接决定了最后一段的结构和长度。处理完最后一段，问题规模缩小，形成递归/贪心结构。

**判断流程**：
1. 观察每种段的定义，找出**结尾字符**与**段长**的对应关系。
   - 如音节题：元音结尾 → 长度 2，辅音结尾 → 长度 3。
2. 用一个指针 `i` 从字符串末尾 (`n-1`) 开始。
3. 循环，直到 `i < 0`：
   - 看 `s[i]`，根据规则确定当前段的长度 `len`。
   - 截取 `s.substr(i - len + 1, len)`。
   - `i -= len`。
4. 将收集到的段反转后输出（因为是从后往前收集的）。

**代码模板**：

```cpp
vector<string> parts;
int i = n - 1;
while (i >= 0) {
    if (/* 根据 s[i] 判断段长 */) {
        int len = /* 对应的长度 */;
        parts.push_back(s.substr(i - len + 1, len));
        i -= len;
    }
}
reverse(parts.begin(), parts.end());
// 用分隔符连接输出
```

**常见陷阱**：
- 用 `for (i = n-1; i >= 0; i--)` 会导致 `i` 每回合自动减 1，而你需要减 2 或 3，造成错乱。**必须用 `while` 手动控制**。
- 确保 `i - len + 1 >= 0`（题目通常保证合法，否则需要边界判断）。
- 如果收集的顺序是反的，记得 `reverse`。

---

**变种扩展**：
- 从前往后处理何时可行？当段的长度由**开头字符**决定时（如开头是特殊标记），从前往后更自然。
- 某些题可能需要同时考虑前后（如回文切割），此时可能需要双向扫描。

---

**一句话总结**：  
看到“按规则分段，规则由段尾决定”的字符串题，立刻想到**从后往前贪心**，用 `while` 手动移动指针，最后反转输出。