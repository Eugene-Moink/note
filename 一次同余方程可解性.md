### 一次同余方程可解性

**关键词**：`倍数取模`,`凑余数`,`能否整除`,`mod`,`同余`

---

**问题模型**：
给定 A, B, C，判断是否存在正整数 x，使得：
\[
A \times x \equiv C \pmod B
\]

**常见出题形式**：

- 选若干个 A 的倍数，总和模 B 能否等于 C
- 每次跳 A 步，在模 B 的环上能否到达位置 C
- 能否用面值为 A 的硬币凑出模 B 等于 C 的金额

---

**充要条件**：
g**cd**(**A**,**B**)∣C
即 A 和 B 的最大公约数能整除 C。

---

**代码**：

```cpp
// 判断 A*x ≡ C (mod B) 是否有正整数解
// 等价于判断 gcd(A, B) 是否能整除 C
if (C % gcd(A, B) == 0)
    cout << "YES";
else
    cout << "NO";
```

---

**直观理解**：
想象一个模 B 的钟面（0 到 B-1），从 0 开始，每次跳 A 格。
你能到达的所有位置，恰好是 gcd(A, B) 的倍数位置。
所以目标 C 必须是 gcd(A, B) 的倍数，否则永远跳不到。

---

**注意事项**：

- 如果题目要求 x 必须是正整数（至少选一个数），只要方程有解，总可以通过加 B/gcd(A,B) 的倍数让 x 变成正数，所以不需要额外判断 x ≥ 1。
- 如果题目要求 x 为非负整数（可以不选），则 C = 0 恒有解（x = 0），无需判断。
