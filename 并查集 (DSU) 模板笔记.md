# 并查集 (DSU) 模板笔记

## 适用场景

- 动态判断**两个元素是否在同一个集合中**（连通性判断）。
- 将**两个不同的集合合并**成一个大集合。
- 作为图论算法的基础（如 **Kruskal 最小生成树**、**克鲁斯卡尔算法**）。

## 核心模板代码

```cpp
#include<bits/stdc++.h>
#define endl "\n"
using ll = long long;
using namespace std;

// 1. 开全局数组：范围至少比最大数据范围大 5~10
const ll MAXN = 200005; 
ll fa[MAXN];

// 2. 初始化：一开始每个人都是独立的
void init(ll n)
{
    // 注意：元素编号通常从 1 开始
    for (ll i = 1; i <= n; i++) 
    {
        fa[i] = i; 
    }
}

// 3. 查找（找祖宗）：带路径压缩
ll find(ll x)
{
    if (fa[x] == x) return x;               // 自己就是老大
    return fa[x] = find(fa[x]);             // 顺手把沿途的节点直接连到老大
}

// 4. 合并：把两个不同的圈子合起来
void merge(ll x, ll y)
{
    ll fx = find(x); // 找 x 的最终老大
    ll fy = find(y); // 找 y 的最终老大
    if (fx != fy)    // 如果老大不是同一个人
    {
        fa[fx] = fy; // 让 x 的老大认 y 的老大当爹
    }
}
```

## 关键注意事项（避坑指南）

- **严重必填**：**必须调用 `init(n);`**！（很多新手样例能过，但提交 WA，就是因为忘了初始化，导致所有 `fa[i]` 默认指向 0）。
- **数组大小**：数组一定要比题目给定的 `n` 大。例如 \(n \le 10^5\)，开 `100005`；**为了防边界，建议直接开 `200005`**。
- **下标陷阱**：题目里的人名/编号通常从 `1` 开始，所以 `init` 里的循环必须是 `for (ll i = 1; i <= n; i++)`，**不要写成 `i < n`**。
- **路径压缩**：`find` 函数最后一行一定要写 `return fa[x] = find(fa[x]);`。如果只写 `return find(fa[x]);`，数据量一大会**超时 (TLE)**。

## 推荐练习题目

- **`P3367 【模板】并查集`**：纯粹的模板题，练手速。
- **`P1551 亲戚`**：模板的直接套用，练输入流分离（合并操作和查询操作分两段输入）。
- **`P1111 修复公路`**：需要配合 `sort` 一起使用的进阶题（为 **最小生成树** 打基础）。
