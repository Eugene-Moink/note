
**Problem**：给定被打乱的一行整数，其中包含原网格的 `n` 和 `m` 以及 `n*m` 个网格值，求任意一组可能的 `n` 和 `m`。

**Algorithm**：哈希表计数 + 枚举整除性

**Key Points**：

- **读入与统计**：用 `map` 统计每个数字出现次数；需保存原始 `k` 值，避免被 `while(k--)` 修改导致后续计算错误。
- **枚举候选**：遍历 `cnt` 中不同的数字作为候选 `n`，计算 `m = (k-2)/n`，前提是 `(k-2) % n == 0`。
- **合法性检查**：
  - 如果 `n == m`，要求 `cnt[n] >= 2`（因为需要两个相同的数分别作 `n` 和 `m`）
  - 如果 `n != m`，只需检查 `cnt.count(m)` 为真即可（`cnt[n]` 必然 ≥1）
- **输出**：找到第一组合法 `(n, m)` 后立即输出并返回，题目保证有解，任意一组均可。

**Fix**：用 `map` 统计频次，枚举候选 `n`，检查 `m` 是否存在且出现次数满足条件，找到后直接输出。




**Problem**：黑板上初始写着一个整数 \(X\)。接下来有 \(Q\) 个查询，每次查询给出两个整数 \(A_i\) 和 \(B_i\)，将它们添加到黑板上。每次添加后，黑板上数字总数变为 \(2i+1\)（奇数），要求输出当前所有数字的中位数。数据范围 \(Q \le 2\times10^5\)，单次查询必须控制在 \(O(\log Q)\) 内。

**Algorithm**：双堆法（Two Heaps）动态维护中位数。

1. 定义一个大根堆 `L`（左堆），存储当前所有数中较小的那一半，堆顶是这一半的最大值（即中位数）。
2. 定义一个小根堆 `R`（右堆），存储当前所有数中较大的那一半，堆顶是这一半的最小值。
3. 保持平衡约束：**`L.size() == R.size() + 1`**。因为总数始终是奇数，这样左堆的堆顶元素就是全局中位数。

**Key Points**：

- 初始将 `X` 推入 `L`，此时 `L.size() = 1`，`R.size() = 0`，满足平衡约束。
- 插入逻辑：读取 `A_i` 和 `B_i` 后，逐个与 `L.top()` 比较：
  - 若新值 `<= L.top()`，说明它属于“较小的一半”，插入 `L`。
  - 否则，说明它属于“较大的一半”，插入 `R`。
- 平衡调整（插入每次新值后立即检查）：
  - 如果 `L.size() < R.size() + 1`：将 `R.top()` 移到 `L`。
  - 如果 `L.size() > R.size() + 1`：将 `L.top()` 移到 `R`。
- 输出：每次查询处理完两个数的插入和平衡后，直接输出 `L.top()` 作为答案。

**Fix**：

- 不要使用 `while` 循环调整堆大小，因为每次插入一个数，最多只会打破一次平衡，两个 `if` 条件判断即可。
- 插入时的比较条件必须写成 `<=`，确保相等值的数字能正确放入左堆，避免相等情况下中位数取错。
- 输出的语句必须放在 `while(Q--)` 查询循环的内部，不能在循环结束后统一输出。
- 所有变量用 `long long` 存储，保证 \(10^9\) 范围的数据在比较和计算时不溢出。
