# 📘 独立贡献贪心问题 解题模板与思路

## 一、 适用场景与题型特征
当题目同时满足以下 **3 个条件** 时，立刻套用此模板：
1. **可操作**：允许对每个元素执行**最多一次**翻转/修改操作（如变方向、改数值）。
2. **独立贡献**：翻转当前元素带来的收益（或损失），**不影响其他元素本身的收益**。
3. **求极值**：允许最多 \(k\) 次操作，问最终**总分最大值**是多少。

---

## 二、 核心数学模型（一句话总结）
> **最终最大总分 = 初始总分 + 前 \(k\) 个最大正向贡献差值之和**

*注意：如果排名前 \(k\) 的贡献差值中有负数或零，因为题目说“最多 k 次”，你可以选择不操作，所以**只累加正数**差值。*

---

## 三、 通用 C++ 代码模板（可默写）

```cpp
#include<bits/stdc++.h>
#define endl "\n"
using ll = long long;
using namespace std;

void moink()
{
    int n;
    string s; // 通常是一串字符或数组
    cin >> n >> s;

    // 1. 【算底分】：计算不做任何操作时的初始总分
    ll base_score = 0;
    vector<ll> gains; // 存放每个位置的翻转收益

    for (int i = 0; i < n; i++)
    {
        // 根据题目修改这里的 左/右 贡献计算
        ll left_count = i;
        ll right_count = n - 1 - i;
        
        if (s[i] == 'L') 
        {
            base_score += left_count; 
            gains.push_back(right_count - left_count); // 计算贡献差（反转后的贡献 - 原来的贡献）
        } 
        else // 'R'
        {
            base_score += right_count;
            gains.push_back(left_count - right_count);
        }
    }

    // 2. 【按降序排序】：贪心，永远优先拿最大的收益
    sort(gains.rbegin(), gains.rend());

    // 3. 【前缀累加】：只有正收益才值得操作
    for (int k = 1; k <= n; k++)
    {
        // 只要还有未取的收益，且下一个收益是正数，就累加进去
        if (k - 1 < gains.size() && gains[k - 1] > 0)
        {
            base_score += gains[k - 1];
        }
        cout << base_score << " ";
    }
    cout << endl;
}
```

---

## 四、 模板变种题（举一反三）

遇到以下变种，**换汤不换药，只需修改 `gains.push_back()` 里面的差值公式**：

1. **翻硬币题**：正面得 \(10\) 分，反面得 \(5\) 分，最多翻 \(k\) 次。
   * `gains.push_back( (翻转后分数) - (原来分数) );`
2. **换赛道题**：有 \(n\) 条赛道，初始在一个赛道，换到另一个赛道有额外加分。
   * `gains.push_back( (赛道B分数) - (赛道A分数) );`
3. **数字翻转题**：把数组里的 \(a_i\) 变成 \(x - a_i\)，求数组最大总和。
   * `gains.push_back( (x - a_i) - a_i );`

---

## 五、 ⚠️ 考场上必看的 3 条防坑指南

1. **排序方向千万不能反！**
   如果你写成 `sort(gains.begin(), gains.end());`，那就是从小到大，你会把最小的收益优先加上去，直接 WA。
   ✅ **正确写法必须加 `r`**：`sort(gains.rbegin(), gains.rend());`

2. **负数不能加！**
   由于题目说“最多 \(k\) 次”，翻转负数收益会扣分。你可以在累加前强制判断 **`if (gain > 0)`**，一旦在降序数组中发现小于等于 0 的增益，后面的就全都不用看了（因为已经排序，后面肯定全是负数或 0）。

3. **防溢出（统一 `ll`）**
   计算 `left_count` 和 `right_count` 以及最终答案时，很有可能超过 \(2 \times 10^9\) 的 `int` 上限。**所有的累加容器 `gains` 和答案 `base_score`，必须使用 `long long`**。